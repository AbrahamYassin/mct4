import './globals.css'
export const metadata = { title:'MyCVTop — SaaS CV', description:'Créez, gérez et exportez vos CV' }
export default function RootLayout({ children }){ return <html lang="fr"><body>{children}</body></html> }
